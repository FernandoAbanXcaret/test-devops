apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gx-xibalba.fullname" . }}-test-connection"
  labels:
    {{- include "gx-xibalba.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gx-xibalba.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
