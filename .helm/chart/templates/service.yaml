apiVersion: v1
kind: Service
metadata:
  name: {{ include "gx-xibalba.fullname" . }}
  labels:
    {{- include "gx-xibalba.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "gx-xibalba.selectorLabels" . | nindent 4 }}
